Problem1:
/*For loop example
const numbers = [1, 2, 3, 4, 5, 6]
let sum = 0;
for(let count = 0; count < numbers.length; count++)
{
	sum += numbers[count];
}

console.log("numbers summed = " + sum);
*/

const numbers = [1, 2, 3, 4, 5, 6]
let sum = 0;
let count = 0;
while(count < numbers.length)
{
	sum += numbers[count]
	count++;
}